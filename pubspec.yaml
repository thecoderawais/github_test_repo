name: alerts_app_ui_sample
description: A new Flutter application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_statusbarcolor: 0.2.3
  simple_animations: 1.3.8
  cached_network_image: ^2.3.1
  flutter_svg: 0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

  assets:
   - images/theme6/
